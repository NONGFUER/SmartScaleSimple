# 2026-08-15

## 双路称重改造完成
- 左=/dev/ttyAMA0、右=/dev/ttyAMA1，两路 9600 8N1、slave=1、Feigong 协议。
- WeightSensorWorker/WeightSensor 构造函数加 (portName, baud=9600, slave=1) 参数；显式传参优先级高于环境变量，不传保持旧行为（SMARTSCALE_SERIAL_PORT→ttyAMA0）。
- main.cpp 建双实例，注册 QML 单例 WeightManagerLeft/WeightManagerRight；右路可用 SMARTSCALE_SERIAL_PORT_R 覆盖设备节点。
- Main.qml 左右模块分别绑定 Left/Right，布局未动（用户已自行修复对称：间距104/列宽340=卡片宽340）。
- 编译通过（build/appSmartScale）。上板验证：两路独立显示/去皮/归零，日志中 Modbus TX 带 portName 可区分通道。
